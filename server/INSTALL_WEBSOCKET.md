# Instalar Socket.IO para WebSocket

Ejecuta en la terminal del servidor:

```bash
cd server
npm install socket.io
npm install socket.io-client  # Para testing
```

¡Listo! Continúa con el siguiente paso.
